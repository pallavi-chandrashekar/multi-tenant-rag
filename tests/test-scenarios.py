# This file is superseded by test_e2e.py which covers the same scenarios
# as proper pytest integration tests (marked `integration`).
#
# Run with:
#   docker-compose up -d
#   pytest tests/test_e2e.py -v -m integration
